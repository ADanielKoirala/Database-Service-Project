# Frontend

React + Vite client for the Secure DBaaS demo.

## Run

```bash
npm install
npm run dev
```

The frontend expects the Flask API to be running at `http://127.0.0.1:5000`.

## Screens

- Home page: project overview
- Login/Register: authentication flow
- Dashboard: role-filtered patient data, user admin tools, and Merkle integrity metadata
